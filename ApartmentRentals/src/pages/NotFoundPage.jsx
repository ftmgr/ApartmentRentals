import React from 'react';

//Not sure to use this for now. But if there is nothing to show, could be?

const NotFoundPage = () => {
    return (
        <div>
            <h1>Not Found Page</h1>
            {/* Render your items list here */}
        </div>
    );
};

export default NotFoundPage; // Ensure you have this line